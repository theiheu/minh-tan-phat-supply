import { PagePlaceholder } from "@/components/page-placeholder";

export default function DefectsPage() {
  return (
    <PagePlaceholder
      title="Vật tư hỏng"
      description="Danh sách phiếu ghi nhận vật tư hỏng và luồng hỏng → sửa → nhập lại/thanh lý."
    />
  );
}
