import { PagePlaceholder } from "@/components/page-placeholder";

export default function NewDefectPage() {
  return (
    <PagePlaceholder
      title="Ghi nhận vật tư hỏng"
      description="Chọn kho nguồn, thêm dòng vật tư hỏng (số lượng, chi tiết, kiểu, mức độ, ảnh)."
    />
  );
}
