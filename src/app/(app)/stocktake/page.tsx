import { PagePlaceholder } from "@/components/page-placeholder";

export default function StocktakePage() {
  return (
    <PagePlaceholder
      title="Kiểm kê"
      description="Chọn kho, nhập số thực tế, hiển thị lệch ± và chốt kiểm kê tạo điều chỉnh."
    />
  );
}
