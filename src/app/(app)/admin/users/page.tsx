import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminUsersPage() {
  return (
    <PagePlaceholder
      title="Quản trị · Người dùng"
      description="Danh sách profile, invite (email + role + zone), đổi role, vô hiệu hóa."
    />
  );
}
