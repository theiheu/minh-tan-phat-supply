import { PagePlaceholder } from "@/components/page-placeholder";

export default function TransfersPage() {
  return (
    <PagePlaceholder
      title="Chuyển kho & Điều chỉnh tồn"
      description="Chuyển kho giữa các vị trí và điều chỉnh tồn thủ công (bắt buộc lý do)."
    />
  );
}
